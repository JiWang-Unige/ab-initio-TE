# Strong-mask controls: terminal status

Live Slurm and native artifacts checked on 2026-09-23 at 22:42 UTC show that
the RM2 arm did not finish within its frozen 24-hour pooled-mask limit.
This is an interpretable resource-limited terminal state, not a completed
two-arm utility comparison.

| stage | observed terminal state | implication |
|---|---|---|
| RM2 pooled discovery, 12856283 | TIMEOUT, 24h00m29s; 4 CPU, 64 GB | 96.03 allocated CPU-hours; no complete classified library or mask |
| RM2 receiver, 12857410/12857411 | CANCELLED by scheduler, never started | 0/20 receiver cells; gene metrics unavailable |
| original final score, 12857412 | CANCELLED by scheduler, never started | no complete RM2-versus-D/R_TE comparison |
| RED and frozen controls | previously completed and qualified | retain the same 639-locus results and all gain/loss sets |

The native log reached RECON edge redefinition after an 11h05m54s element
redefinition stage. The preserved work directory contains intermediate
`consensi.fa` files but no `consensi.fa.classified`. These are not substituted
for the complete discovered library required by the protocol. No RepeatMasker
mask or downstream RM2 receiver result was produced. The original native
`status.json` still says `RUNNING` because scheduler termination bypassed the
wrapper's final write; Slurm's TIMEOUT and its time-limit stderr establish
the actual terminal state.

The full failed run and its cost are retained. No larger-budget retry is
automatically submitted: the frozen resources specify at most 24 hours for
this pooled mask. The public job was never restarted merely to change its
partition.

Available RED/D/U_soft/R_TE F1 values remain 0.60103/0.59840/0.55743/0.59173.
The earlier D-minus-RED interval includes zero; RM2's timeout adds no evidence
for either method's accuracy superiority or equivalence. RM2 metrics and its
primary planned contrasts are explicitly null in
`compact/terminal-20260924/terminal.json`, which also links the preserved
partial result containing all fixed controls and gain/loss sets. The original
partial artifact is not relabeled as a successful final scientific result.
