# Platypus strong-mask follow-up status

**Terminal update (2026-09-24 local date):** the old RM2 job reached its
24-hour TIMEOUT; its receiver and score dependencies were cancelled without
starting. The running/pending descriptions below are historical snapshots.
See [terminal record](TERMINAL-20260924.md) for preserved costs and explicit
unavailable RM2 metrics. No larger-budget retry was submitted.

This note records execution state separately from the frozen final score. The
completed RED arm is eligible for a partial, same-receiver score; the RM2 arm
remains an independent strong traditional-mask arm and is not represented by a
zero or an inferred value.

| component | job/state | interpretation |
|---|---|---|
| pooled RED native mask | `12856282`, completed | 20/20 fixed core+halo records; all-repeat mask, no family claim |
| RED receiver | `12857061_0` smoke plus `12857204_[1-19%1]`, completed | six-channel observer and native GTF/GFF3 shape passed |
| pooled RM2 native mask | `12856283`, running on `public-longrun-cpu` when checked | preserve the running job and its partial files; no score yet |
| RM2 receiver smoke/array/final score | `12857410_0`, `12857411_[1-19%1]`, `12857412` | pending `afterok` chain; all three pending jobs were migrated in place to `private-teodoro-gpu` |

The migration changed only the scheduler partition for jobs that had not
started. Scientific inputs, checkpoints, threshold, receiver, panel geometry,
and scoring endpoints remain those in the frozen protocol. Native pending jobs
retain their dependencies; the final score will still require all twenty RM2
receiver cells.

## Two-GPU-lane coordination (2026-09-18)

The pending first RM2 receiver job `12857410_0` was updated in place to
`afterok:12856283,afterany:12898018`. Its scientific prerequisite remains the
successful original RM2 mask. The additional `afterany` on the fixed class-map
array only serializes GPU use: this receiver and its existing downstream
array now follow the representation/class-map lane, while whole-genome D
inference occupies the other lane. The class-map score is not a selection
condition for the receiver.

The live scheduler accepted this dependency with the job still PENDING.
The initial serialization used original class-map array `12890969`; after
that array failed its span-projection assertion, the receiver was briefly held
while repaired array `12898018` was submitted, then rewired and released.
The repaired class-map array follows length-only retry `12897975`.
There was no replacement job, cancellation, restart, parameter change, or
extra GPU allocation. The old `public-longrun-cpu` RM2 job `12856283` remains
running; `12857411` and `12857412` retain their existing successful-receiver
dependencies. If an upstream failure prevents class-map from reaching a
terminal state, its existing dependency must be repaired before this
serialization gate can release; failure is not treated as a scientific result.

The separate `partial_score.py` writes
`outputs/PLATYPUS-STRONG-MASK-CONTROLS-20260917/score/partial-red-20260918/result.json`
with status `PARTIAL_RED_ONLY_RM2_PENDING`. It reuses 120 frozen control cells,
uses the 100 Mb gene-scoring denominator, records the 104 Mb mask-panel
denominator, and leaves the final `score/result.json` untouched.

The same terminal RED result is compact-synchronized locally at
`reports/PLATYPUS-STRONG-MASK-CONTROLS-20260917/compact/partial-red-20260918/result.json`.
The pending RM2 receiver chain remains gated by the native RM2 mask job and
will not be represented by a zero, imputed score, or a RED-only final claim.

## Partial RED result (terminal)

The partial scorer completed as job `12888155` in 9 seconds. The exact frozen
receiver counts are:

| arm | TP | FP | FN | precision | recall | F1 |
|---|---:|---:|---:|---:|---:|---:|
| RED | 409 | 313 | 230 | 0.56648 | 0.64006 | 0.60103 |
| D | 412 | 326 | 227 | 0.55827 | 0.64476 | 0.59840 |
| U_soft | 381 | 347 | 258 | 0.52335 | 0.59624 | 0.55743 |
| U_nosm | 358 | 348 | 281 | 0.50708 | 0.56025 | 0.53234 |
| R_TE | 408 | 332 | 231 | 0.55135 | 0.63850 | 0.59173 |
| R_all | 405 | 329 | 234 | 0.55177 | 0.63380 | 0.58995 |
| P | 408 | 338 | 231 | 0.54692 | 0.63850 | 0.58917 |

These values are valid for the fixed 639-locus receiver panel, while the
traditional RED mask itself has no family classification. The final two-arm
comparison remains pending RM2 and will be written to the original final score
location only after all RM2 receiver cells pass.
