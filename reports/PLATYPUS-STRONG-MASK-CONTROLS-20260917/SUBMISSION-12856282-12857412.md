# Submission record

The pooled traditional-mask and fixed-receiver jobs were submitted on 2026-09-17. Scientific inputs, tool versions, thresholds, receiver configuration, and scoring endpoints are frozen in the experiment protocol.

| stage | job | dependency / state at submission |
|---|---:|---|
| Red pooled native mask | 12856282 | completed; 20/20 contig manifests, 464.7 s |
| RM2 pooled native mask | 12856283 | running when this record was written; RepeatModeler all-by-other stage |
| Red c00 receiver smoke, first attempt | 12856827_0 | failed before model inference due loader import bug; preserved under remote `tiberius/RED/c00` |
| Red c00 receiver smoke, fixed retry | 12857061_0 | completed; six-channel observer and native GTF/GFF3 passed; remote root `tiberius-r2/RED/c00` |
| Red c01-c19 receiver | 12857204_[1-19%1] | pending resources; one GPU cell at a time |
| RM2 c00 receiver smoke | 12857410_0 | afterok RM2 mask 12856283 **and** RED receiver array 12857204; enforces one new GPU arm at a time |
| RM2 c01-c19 receiver | 12857411_[1-19%1] | afterok RM2 c00 smoke 12857410; one GPU cell at a time |
| Final score | 12857412 | afterok Red array 12857204 and RM2 array 12857411 |

The initial shared-cpu mask submissions 12855927 and 12855928 were cancelled before start because that partition rejected the 24-hour request. The replacement pooled jobs use public-longrun-cpu and account-compatible 4 CPU/64 GB execution. The failed first RED smoke is retained as an engineering failure and cannot satisfy the retry dependency.

The dependency graph intentionally blocks downstream cells after an upstream failure; there is no fallback mask, score-driven selection, or parameter change.

RM2's native RepeatMasker invocation is recorded as `-pa 4` under the account-compatible 4-CPU allocation. Because RepeatMasker controls engine batching internally, this run is not used as a strict CPU-thread benchmark or cost claim.
