# Findings Log

> 会话恢复 / 每轮迭代开局**必读**。分两类，避免重复踩坑。
> - **Research Findings**：方法层洞察（影响 /pivot 决策）。
> - **Engineering Findings**：调试/环境层经验（下一轮 baseline 知识）。
>
> 谁写：/result-log(语义成功后提炼) / /implement(smoke 调试经验) / /pursue(每轮) / /note-add(kind=note)。
> 一行一条，标日期 + 来源 exp_id。**不写未验证猜测**（猜测进 wiki/ideas）。

## Research Findings
- 2026-08-11 [BENCH-5TOOL-DENOMINATOR-CLOSURE-20260811-R2] A complete five-cell terminal matrix is not itself a valid negative: post-launch nonzero exits and integration failures must be `INVALID_RUN`, while `FOUNDATIONAL_TYPED_BLOCK` is reserved for pre-execution asset absence. Independent audit reclassified RM/EarlGrey/HiTE/EDTA as invalid and retained only TEtrimmer's immutable-Pfam absence as foundational.
- 2026-08-11 [TEFM-NEW-DIRECTIONS-PILOTS-20260811] The four proposed scientific directions are presently blocked by evidence contracts rather than model performance: F lacks biological truth/comparator/H0 pins, S lacks ontology and homology/clade split materialization, G lacks 5/5 exact anchor run records, and E lacks exact 2,200 bindings plus weights/backend pins. Synthetic/code-gate assets must not be promoted to scientific screen evidence.
- 2026-06-16 [label_source_audit] Self-run RepeatMasker+Dfam vs existing UCSC/RepeatMasker TE overlap is species-dependent: mouse/rat and small anchors show high bp concordance, mammal self-run labels are usually a high-precision subset of UCSC, while chicken/pig/dog/cattle have large UCSC-only TE mass and Xenopus/Tribolium are severe Label-A coverage failures under Dfam-only.
- 2026-06-16 [label_source_audit] UCSC-only bp in the paired audit is mostly bona fide RepeatMasker TE classes (`LINE`, `SINE`, `LTR`, `DNA`, `RC`) rather than simple/tandem/satellite repeats; exception: zebrafish `rmsk_te.bed.gz` contains ~0.80 Mb `Satellite?`, so zebrafish UCSC-only delta needs a small non-TE-repeat caveat.
- 2026-06-17 [SELF_LABELA_VS_UCSC_TWO_SPECIES_20260617] Strict-TE merged bp audit on current ready-by-design sources confirms high but non-identical self Label-A vs UCSC concordance: human hs1 Jaccard 94.714% (48.1 Mb UCSC-only, 29.0 Mb self-only), mouse Jaccard 89.247% (106.6 Mb UCSC-only, 13.7 Mb self-only). Mouse supports treating Dfam-only absence as unsafe negative in some regions and using U-shield/concordance checks.
- 2026-06-17 [SELF_LABELA_VS_UCSC_ALL_SPECIES_20260617] All-available source-concordance audit over 24 unique species/assemblies shows three regimes: high/moderate concordance for human assemblies, mouse, fruit_fly, zebrafish, c_elegans, and horse; high-precision self-run subsets with large UCSC-only TE mass for cattle/pig/chicken/opossum; and severe self-run strict-TE failure for plants plus several stress invertebrate/amphibian/reptile entries. Plant self-run Label-A currently yields 0 strict TE bp while comparators contain large strict TE mass, so PlantTE training cannot treat those self-run outputs as usable TE positives without repair.
- 2026-06-17 [plant_dfam_partition_diagnosis] Plant self-run strict-TE zero is caused by library/configuration coverage, not absence of plant TEs or interval-compare parsing: RepeatMasker ran with `-species Oryza sativa`/`Sorghum bicolor`, but the active Dfam 3.9 FamDB has partition 6 `Viridiplantae` absent; generated plant `specieslib` files contain only 9 root artefact/simple-repeat-like sequences (about 16 KB), while mouse has 1380 specieslib sequences. Plant runs completed technically but are invalid as PlantTE Label-A positives.
- 2026-06-18 [PIPE-TEFM-SUPP-20260617] UCSC strict-TE token-proxy screen favors window=4096: H0 best is `ntv2_500m@4096` (TE-F1 0.9458), with `generanno@4096` close (0.9430). B/C transfer is model/context dependent: 2048 mean favors `generanno`, while 4096 paired transfer favors `ntv2_500m`; keep both for bp-level follow-up.
- 2026-06-18 [PIPE-TEFM-SUPP-20260617] Mouse-only fine-tuning generalizes well to close vertebrates under one-chrom screen (`generanno` mean TE-F1 0.8721; `ntv2_500m` 0.8546), but A2 no-human mixture fails on held-out invertebrates despite reasonable vertebrate performance. Do not treat mixture as universal animal TE model without kingdom/clade routing.
- 2026-06-18 [PIPE-TEFM-SUPP-20260617] Non-overlap window edge degradation is real: edge-minus-center is most severe at 512 and some 1024 windows, and still negative at 4096/8192. Final bp-level inference should use overlapping windows with center-weight merging before CRF/HMM/heuristic segment smoothing.
- 2026-06-18 [PIPE-TEFM-SEG-SF-20260618] Overlap plus smoothing is required for annotation usability: raw threshold has high bp-F1 but low segment-F1 (for example 4096 stride 2048 raw segment-F1 0.5021), while HMM-style smoothing raises segment-F1 to about 0.744 with similar bp-F1.
- 2026-06-18 [PIPE-TEFM-SEG-SF-20260618] GENERanno 4096 is the stronger follow-up setting: 4096 superfamily TE-detect F1 0.9393 and all-6 macro-F1 0.7494 outperform 2048 TE-detect F1 0.9326 and all-6 macro-F1 0.7287.
- 2026-06-18 [PIPE-TEFM-SEG-SF-20260618] TE-fragment clustering screen finds basic sequence features plus contrastive projection (C1) outperform current GENERanno embedding variants on balanced UCSC fragments; C1 mean holdout macro-F1 is 0.7405 versus A1 pretrained GENERanno+contrastive 0.6436.
- 2026-06-19 [PIPE-TEFM-REPAIR-20260618] GENERanno 4096 mixed-animal failure is not global: `invert_boost_animal_4096` reaches B-panel mean TE-F1 0.9351 and A1 mean 0.8985, while A2 all-species mean stays about 0.575 because held-out honeybee/beetle and distant stress species dominate the collapse. Treat those as stress/label-source diagnostics, not evidence that animal annotation broadly fails.
- 2026-06-19 [PIPE-TEFM-REPAIR-20260618] For interval-quality TE annotation, threshold-only optimization is still insufficient: raw threshold 0.6 has bp-F1 0.9389 but segment-F1 0.4846, while threshold 0.35 + `hmm_penalty2` has similar bp-F1 0.9385 and much better segment-F1 0.7339 / boundary-F1 0.6181.
- 2026-06-19 [PIPE-TEFM-REPAIR-20260618] Embedding contradiction is metric/protocol-specific: A1 pretrained GENERanno embeddings are strong under pairwise/linear-probe diagnostics (length 512 holdout macro-F1 0.8495, pair AUC 0.9528), but B1 binary token fine-tuned embeddings are weak for superfamily clustering (length 512 holdout macro-F1 0.5561). Do not use binary TE fine-tuning alone as the representation-learning objective.
- 2026-06-29 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260629_V6] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison from the deduplicated current manifest and reproduced the same species-level overlap tables as `RERUN_20260629_V4` (`summary.tsv` and `qc_flags.tsv` are byte-identical). The current evidence remains 24 paired entries with 7 high, 2 moderate, 5 low, and 10 severe concordance classes; six PlantTE species still lack strict comparator files (`setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, `green_foxtail`). During the rerun audit, the only ready-view path defect was six broken pig genome symlinks pointing at a nonexistent `susScr11.fa.gz`; these were repaired to the existing `susScr11.fa`, leaving `02_ready_by_design` with 0 broken symlinks.
- 2026-06-30 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260630] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison from a fresh species-deduplicated pair manifest. The result is unchanged from `RERUN_20260629_V8B` and `RERUN24_20260621` for core overlap fields (`changed_rows=0`): 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe. `02_ready_by_design` remains clean with 803 symlinks, 424 self Label-A links, 273 UCSC comparator links, 106 genome links, and 0 broken links. Root-level `RM_*` directories are absent; quarantined `RM_*` scratch directories are tiny and cleanup-safe if no forensic inspection is needed.
- 2026-06-29 [DENOVO_B_UCSC_PART27_REMAP_20260629] The first 27-row de novo-vs-UCSC animal partial benchmark contained three false zeros caused by seqid mismatch in standardized annotations rather than tool failure. `compare_denovo_ucsc.py` now infers remap rules from EDTA `id_mapping.json` and EarlGrey `*.fa.prep -> genome.fa` header order when the finalized annotation still uses renamed contig ids. Recomputed partial results change exactly three rows: `cattle EDTA 0.000000 -> 0.650028`, `horse EDTA 0.000000 -> 0.420504`, and `cattle EarlGrey 0.000000 -> 0.735879`; all other 24 rows are unchanged. This lifts the current partial means to `repeatmodeler 0.406356`, `repeatscout 0.359072`, `edta 0.330296`, and `earlgrey 0.514781` over its three completed rows. Do not use `reports/de_novo_benchmark/DENOVO_B_UCSC_PART27_20260629` as final evidence; use `.../DENOVO_B_UCSC_PART27_REMAP_20260629` instead.
- 2026-06-19 [SELF_LABELA_VS_UCSC_FULLPARTITIONS_20260619] Full-partition RepeatMasker+Dfam rerun fixes the previous plant/invertebrate strict-TE-zero failure, but source concordance remains species-dependent. Human assemblies are high concordance (`human`/hs1 Jaccard 0.948, hg38 0.912, hg19 0.892); mouse, fruit_fly, c_elegans, zebrafish, and horse remain usable-to-strong. Plants are no longer zero but mostly self-run subsets of comparator annotations (for example maize Jaccard 0.095, rice 0.043, soybean 0.122), and amphibians remain severe failures (`western_clawed_frog` 84 kb self strict TE vs 456 Mb comparator; `x_laevis` 57 kb vs 239 Mb). Use the concordance matrix for U/RN masking and do not treat self-run absence as reliable negative in low-concordance species.
- 2026-06-20 [SELF_LABELA_VS_UCSC_CURRENT_READY_20260620] Re-running the concordance audit from the current `02_ready_by_design` entrypoint gives the same final full-partition label-source picture over 24 paired species/assemblies: 8 high-concordance entries (`human` hs1, hg38, hg19, mouse, fruit_fly, zebrafish, c_elegans, horse), 4 moderate entries (pig, cattle, chicken, opossum), 5 low plant entries (soybean, brachypodium, maize, teosinte, sorghum), and 7 severe entries (`western_clawed_frog`, `x_laevis`, honeybee, beetle, lizard, rice, thale_cress). Six ready species still lack comparator files: setaria_italica, tomato, wild_rice, arabidopsis_lyrata, grape, and green_foxtail.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_LOWCPU_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE audit from the latest `02_ready_by_design` manifest. The result remains stable: 24 paired entries, 8 high, 4 moderate, 5 low, and 7 severe concordance classes; top entries are human hs1 Jaccard 0.948, hg38 0.912, mouse 0.892, hg19 0.892, and fruit_fly 0.889, while lizard, beetle, honeybee, X. laevis, and X. tropicalis remain severe. The failed 12-process attempt is marked partial; the completed evidence directory is `reports/repeatmasker_dfam/SELF_LABELA_VS_UCSC_CURRENT_READY_LOWCPU_20260621`.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN2_20260621] Re-ran the latest current ready-by-design self Label-A vs UCSC/local strict-TE audit from the de-duplicated `MANIFEST_ALL.tsv` after the user requested another check. The result is unchanged from the low-CPU audit: 24 paired entries, 8 high, 4 moderate, 5 low, and 7 severe concordance classes. `02_ready_by_design` has 424 self Label-A links pointing to `RMDFAM_FULLPARTITIONS_RERUN_20260617` and 0 broken symlinks; root-level `RM_*` scratch directories remain quarantined under `99_internal/rm_scratch_quarantine_20260619/`.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN3_20260621] Recomputed the current ready-by-design self Label-A vs UCSC/local strict-TE audit from `MANIFEST_ALL.tsv` using 24 unique paired species/assemblies. Numeric overlaps are stable versus prior current-ready audits; under the explicit strict class threshold used in the new `qc_flags.tsv` (`high` = Jaccard >= 0.80), the classes are 7 high, 5 moderate, 5 low, and 7 severe, with horse at Jaccard 0.791 therefore moderate rather than high. `02_ready_by_design` currently has 803 total symlinks, 424 self Label-A symlinks, all 424 self Label-A links resolving to `RMDFAM_FULLPARTITIONS_RERUN_20260617`, and 0 broken symlinks. Root-level `RM_*` directories are absent from both project roots and the quarantined scratch copy is only about 60K, so it is cleanup-safe if no manual forensic inspection is needed.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN8_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE audit again from `MANIFEST_ALL.tsv` after a failed 4-process RERUN7 attempt. The completed single-process RERUN8 report covers 24 paired species/assemblies and reproduces the stable explicit classes: 7 high, 5 moderate, 5 low, and 7 severe; six ready species still lack comparator files (`setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, `green_foxtail`). `02_ready_by_design` remains correctly allocated by design/role, with 803 total symlinks, 424 self Label-A symlinks, 0 broken symlinks, and all self Label-A links resolving to `RMDFAM_FULLPARTITIONS_RERUN_20260617`. Root-level `RM_*` directories remain absent; the quarantined `RM_892559.ThuJun180438082026` scratch directory is cleanup-safe if no forensic inspection is needed.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN10_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE audit from a species-deduplicated manifest. The report covers 24 paired species/assemblies; explicit threshold classes are 7 high, 2 moderate, 5 low, and 10 severe. Numeric ordering is stable: human hs1/hg38/hg19, mouse, fruit fly, zebrafish, and C. elegans are high; horse and pig are moderate; cattle/chicken/opossum plus soybean/brachypodium are low; maize/teosinte/sorghum/thale_cress/rice plus lizard/beetle/honeybee/X. laevis/X. tropicalis are severe. Six ready species lack comparator files. `02_ready_by_design` remains clean with 803 symlinks, 424 self Label-A links, 0 broken links, and self Label-A resolving to `RMDFAM_FULLPARTITIONS_RERUN_20260617`; root `RM_*` directories are absent and quarantined scratch copies are cleanup-safe if no forensic need remains.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN11_20260621] Repeated the current ready-by-design self Label-A vs UCSC/local strict-TE comparison using a fresh species-deduplicated pair manifest and `--jobs 2`. Results are stable over 24 paired entries: 7 high, 2 moderate, 5 low, and 10 severe by explicit Jaccard thresholds. The ready-view audit remains clean with 803 total symlinks, 424 self Label-A links, 0 broken links, and all self Label-A links resolving to `RMDFAM_FULLPARTITIONS_RERUN_20260617`; `RM_892559.ThuJun180438082026` is only a 60K quarantined probe/batch scratch directory, not a final annotation product.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN12_20260621] Recomputed the current ready-by-design self Label-A vs UCSC/local strict-TE comparison again from a fresh species-deduplicated `MANIFEST_ALL.tsv` pair manifest. The new report is byte-level stable for core overlap fields relative to RERUN11 (`changed_fields=0`): 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe. The ready-view allocation remains valid with 803 symlinks, 424 self Label-A links, 424/424 resolving to `RMDFAM_FULLPARTITIONS_RERUN_20260617`, and 0 broken links. Report path: `reports/repeatmasker_dfam/SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN12_20260621`.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN13_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison from a species-deduplicated `MANIFEST_ALL.tsv` after the user requested another current annotation check. The report covers 24 paired species/assemblies and reproduces the stable explicit classes: 7 high, 2 moderate, 5 low, and 10 severe. Top concordance entries are hs1/hg38/mouse/hg19/fruit_fly/zebrafish/c_elegans; severe entries include maize/teosinte/sorghum/thale_cress/rice/lizard/beetle/honeybee/X. laevis/X. tropicalis. `02_ready_by_design` remains correctly allocated with 803 symlinks, 424 self Label-A links, 0 broken links, and all self Label-A links resolving to `RMDFAM_FULLPARTITIONS_RERUN_20260617`; root-level `RM_*` scratch dirs are absent and quarantined copies are cleanup-safe if no forensic inspection is needed. Report path: `reports/repeatmasker_dfam/SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN13_20260621`.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN17_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison again using a fresh species-deduplicated pair manifest from `02_ready_by_design/manifests/MANIFEST_ALL.tsv`. The completed single-process RERUN17 report is byte-stable versus RERUN14 for core overlap fields (`changed_fields=0`): 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe; six ready species still lack strict comparator files (`arabidopsis_lyrata`, `grape`, `green_foxtail`, `setaria_italica`, `tomato`, `wild_rice`). RERUN15 and RERUN16 are marked failed partial and must not be used as evidence. The ready-by-design allocation remains clean: 803 symlinks, 0 broken, split by design and `fine_tune`/`eval_only`; `RM_892559.ThuJun180438082026` is only a 60K quarantined RepeatMasker probe scratch directory and is cleanup-safe if no forensic/debug need remains.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN19_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison on `shared-bigmem` after local/low-memory attempts were killed or failed before evidence generation. RERUN19 uses a fresh species-deduplicated pair manifest from `02_ready_by_design/manifests/MANIFEST_ALL.tsv`; `summary.tsv` is byte-identical to RERUN14 and reproduces 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe classes. Six ready species still lack strict comparator files (`arabidopsis_lyrata`, `grape`, `green_foxtail`, `setaria_italica`, `tomato`, `wild_rice`). The ready-by-design allocation remains clean with 803 symlinks, 424 self Label-A, 273 UCSC comparator, 106 genome links, and 0 broken links; all self Label-A links resolve to `RMDFAM_FULLPARTITIONS_RERUN_20260617`. `RM_892559.ThuJun180438082026` is present only in `99_internal/rm_scratch_quarantine_20260619`, is 60K, and contains probe/scratch files rather than final annotations.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN20_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison from a fresh species-deduplicated pair manifest after the user requested another current-result check. The report covers 24 paired species/assemblies and reproduces the stable explicit classes: 7 high, 2 moderate, 5 low, and 10 severe. Top entries are hs1/hg38/mouse/hg19/fruit_fly/zebrafish/c_elegans; severe entries remain maize/teosinte/sorghum/thale_cress/rice/lizard/beetle/honeybee/X. laevis/X. tropicalis. The ready-by-design allocation remains valid with 803 symlinks, 424 self Label-A links resolving to `RMDFAM_FULLPARTITIONS_RERUN_20260617`, 273 UCSC comparator links, 106 genome links, and 0 broken links. `RM_892559.ThuJun180438082026` is present only as a tiny quarantined scratch directory, not a final annotation product.
- 2026-06-21 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN24_20260621] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison after the user requested another check of the current annotation results. RERUN21 was killed by Slurm before final tables, and RERUN22/RERUN23 failed at startup, so all three are marked failed partial and must not be used as evidence. RERUN24 completed with a per-species process strategy and reproduces the stable 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe classes; `qc_flags.tsv` is byte-identical to RERUN19 and core overlap fields changed by 0. Six ready species still lack strict comparator files (`setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, `green_foxtail`). `02_ready_by_design` remains clean with 803 symlinks and 0 broken links; root-level `RM_*` directories are absent, and `RM_892559.ThuJun180438082026` is only a 60K quarantined scratch/probe directory.
- 2026-06-19 [PIPE-TEFM-LOCK-20260619] Primary/stress panel separation is mandatory for animal TE claims: stress species-specific fine-tuning recovers lizard TE-F1 0.2187->0.9516, X. laevis 0.3362->0.9343, and honeybee 0.2545->0.8578 on the same held-out stress split, while beetle remains poor 0.0043->0.0714. This supports a domain/label-shift and source-completeness explanation for most stress failures, not a global animal-model failure.
- 2026-06-19 [PIPE-TEFM-LOCK-20260619] Five-class superfamily should use main4+Unknown/reject with base-pretrained initialization. Base-pretrained SF5 reaches TE-F1 0.9041, main4 conditional macro-F1 0.8644, and Unknown recall 0.3886; binary-H0 initialization keeps similar main4 F1 but collapses Unknown recall to 0.0426, consistent with binary TE fine-tuning damaging open-set/superfamily geometry.
- 2026-06-19 [PIPE-TEFM-LOCK-20260619] Fragmentation metrics are now multi-species screen metrics (6 primary + 4 stress, 600 windows/species), not single-species metrics. On the primary panel, HMM smoothing improves raw segment-F1 0.2392 to 0.5516 and boundary-F1 0.1916 to 0.4430 while reducing short predicted fragments from 0.7898 to 0.0879, but interval quality remains below bp-F1 and needs boundary/segment-specific modeling.
- 2026-06-19 [PIPE-TEFM-LOCK-20260619] Embedding representation claims must keep C1 and A1 baselines. C1 basic sequence features + contrastive is strongest (ARI 0.9208, holdout macro-F1 0.8784), A1 pretrained GENERanno + contrastive is strong (ARI 0.7987, holdout 0.8437), and binary fine-tuned embeddings remain weak (B1 holdout 0.5561).
- 2026-06-21 [PIPE-TEFM-EXTEND-20260620] Animal `invert_boost_animal_4096` remains the strongest transfer branch even on plant/cross screens: plant eval mean TE-F1 0.7269 and cross-panel mean TE-F1 0.5914, outperforming plant PU-TV and cross-kingdom PU. Do not promote plant/cross PU as the primary annotation route.
- 2026-06-21 [PIPE-TEFM-EXTEND-20260620] Plant positive-only/PU without trusted negatives overcalls TE: non-TV PU/positive-only has recall 1.0 and low precision; TV plus HMM/CRF-style smoothing reduces fragmentation but remains low interval quality. PU stays a negative ablation or future methodology route, not the current main model.
- 2026-06-21 [PIPE-TEFM-EXTEND-20260620] Strict family-level dynamic-fragment embedding does not support an FM embedding superiority claim. A1 GENERanno+contrastive improves over raw A0, but C1 basic sequence features + contrastive remains strongest at family level; Dfam consensus is still pending because no configured consensus FASTA was available.
- 2026-06-21 [PIPE-TEFM-EXTEND-20260620] Base-pretrained SF5 independently confirms main4+Unknown/reject: main4 conditional macro-F1 0.8547 and Unknown recall 0.3957. Keep base-pretrained initialization for open-set superfamily work.
- 2026-06-21 [PIPE-TEFM-EXTEND-20260620] Generalization decay cannot be explained by genetic distance alone in this screen. Distance-only R2 is 0.1870; adding label Jaccard/source variables raises R2 to 0.5249, so decay claims must include label/source covariates.
- 2026-06-21 [PIPE-TEFM-EXTEND-20260620] Stress anchors are not enough to solve stress species. Vertebrate anchor slightly improves lizard/X. laevis/honeybee but beetle remains near-zero; these targets should remain stress/label-source diagnostics unless repaired with source-specific evidence.
- 2026-06-21 [PIPE-TEFM-CALIB-20260621] Standard supervised plant/cross fine-tuning with reliable negatives is materially different from PU/positive-only: `cross_supervised_4096` reaches plant-fine mean TE-F1 0.8568 and broad mean 0.5786, while PU remains abandoned as a primary route because its failure mode is overcalling without reliable RN/hardN control.
- 2026-06-21 [PIPE-TEFM-CALIB-20260621] Insect-no-beetle calibration rescues honeybee but not beetle under the same eval protocol: honeybee TE-F1 reaches 0.7983, while red flour beetle stays 0.0059. Treat honeybee as calibratable domain/label shift and beetle as hard label/library/domain failure outside primary means.
- 2026-06-21 [PIPE-TEFM-CALIB-20260621] Dfam consensus family-level embedding closes the missing consensus branch. A1 improves over A0, but C1 basic sequence features + contrastive remains much stronger by clustering metrics (ARI 0.7083 vs A1 0.2242), so no FM embedding superiority claim is supported.
- 2026-06-21 [PIPE-TEFM-CALIB-20260621] SF5 Unknown is a strict open-set/ambiguous bucket, not a normal closed-set class: the data builder maps ambiguous, non-main, `?`, `Unknown`, `RC`, and `Retroposon` RepeatMasker classes to Unknown. Low Unknown recall (0.3886-0.3957 in prior base-pretrained SF5 runs) should be reported as incomplete reject calibration, while main4 conditional macro-F1 remains the primary closed-set superfamily metric.
- 2026-06-21 [PIPE-TEFM-CALIB-20260621] Generalization decay needs label/source and panel variables. Distance-only R2 is 0.0396; the full variable set with label/source, TE composition, train-clade coverage, stress, kingdom, and insect indicators reaches R2 0.7407.
- 2026-06-21 [PIPE-TEFM-ANCHOR-20260621] Panel-specific anchors are the correct recommendation frame. `insect_primary_4096` rescues honeybee to TE-F1 0.9465 while preserving fruit fly/C. elegans performance, but red flour beetle remains near-zero across anchors, so beetle should stay a stress/audit species rather than enter primary means.
- 2026-06-21 [PIPE-TEFM-ANCHOR-20260621] Unknown-annotation segments often contain main4-like SF5 signal, but this is an annotation-audit opportunity rather than automatic relabeling: 260 Unknown segments have mean best-main4 fraction 0.4706, Unknown fraction 0.0620, and BG fraction 0.3826. Strict high-score unannotated candidates remain mostly BG under SF5.
- 2026-06-21 [PIPE-TEFM-ANCHOR-20260621] Background-inclusive embedding confirms the earlier conservative representation conclusion: C1 basic sequence features + contrastive remains stronger than GENERanno embedding + contrastive for both BG+main4 and Unknown/high-score screens. FM embedding superiority is still unsupported.
- 2026-06-21 [PIPE-TEFM-ANCHOR-20260621] A deployable anchor selector is promising only as a screening tool. Variables available before target TE annotation can reach useful in-sample fit, but leave-species-out error remains high; annotation-aware variables such as label concordance explain performance but cannot be part of a deployment selector for a new genome.
- 2026-06-29 [PIPE-TEFM-FINAL-20260623] Final model-size/window screen does not support simple parameter-count dominance. Human H0 and animal_fine are led by `ntv2_250m@4096` (human TE-F1 0.93494; animal_fine mean 0.64823), while the best NTv3 H0 row is `ntv3_650m_pre@4096` (0.91962) and the best plant_fine mean is `ntv3_100m_pre@2048` (0.39802). Use panel-specific model/window recommendations rather than one collapsed mean.
- 2026-06-30 [PIPE-TEFM-FINAL-EBAR-20260629] Chromosome-repeat evaluation preserves the panel-specific anchor decision: `ntv2_250m@4096` leads animal_fine across 18 chromosome/species rows (mean TE-F1 0.6431), while `ntv3_100m_pre@2048` leads plant_fine across 15 rows (mean TE-F1 0.3833). Do not collapse animal and plant into one headline mean.
- 2026-06-30 [PIPE-TEFM-FINAL-PLANTQC-20260629] Plant transfer metrics are strongly label-source confounded. Seven plant Label-A vs UCSC/local strict-TE rows have mean Jaccard 0.0784, so low plant strict segment scores must be reported with library/comparator completeness caveats.
- 2026-06-30 [PIPE-TEFM-FINAL-STRICTSEG-20260629] Strict interval thresholds expose a large usability gap hidden by bp-F1. At IoU 0.8 and boundary 5 bp, animal `ntv2_250m@4096` + CRF-style smoothing reaches bp-F1 0.6453 but segment-F1 only 0.2557 and boundary-F1 0.0989; plant `ntv3_100m_pre@2048` + gap100/min100 reaches bp-F1 0.4585 but segment-F1 0.0305. Report bp detection and interval annotation usability separately.
- 2026-06-30 [PIPE-TEFM-FINAL-STRICTSEG-20260629] Smoothing trades off false fragmentation against true-TE retention. For animal `ntv2_250m@4096`, raw threshold has high short-fragment burden (~4359.9 short predicted fragments/row); CRF-style smoothing improves strict segment-F1 and reduces short fragments to ~850.2, while gap100/min100 almost removes short fragments (~5.5) but raises missed true TE rate.
- 2026-06-30 [PIPE-TEFM-FINAL-SELECTOR-20260630] Multi-anchor reporting is empirically stronger than a single universal model in the current screen. Observed best-anchor-per-species mean TE-F1 is 0.7787 across 22 species, while the best broad single model with at least five rows is `cross_supervised_4096` at mean TE-F1 0.5432. Treat this as screen support for panel-specific anchors, not a deployment claim.
- 2026-06-30 [PIPE-TEFM-FINAL-SELECTOR-20260630] NTv2-500M species-specific recovery is a useful soft annotation-quality audit. Red flour beetle remains poor after target fine-tuning (TE-F1 0.1494), and thale cress is also poor (0.4168); soybean (0.5797) and C. elegans (0.7667) are partial/caution. Many low-concordance species still recover strongly, so low source concordance alone is not an exclusion rule.
- 2026-06-30 [PIPE-TEFM-FINAL-SELECTOR-20260630] Deployable anchor selection remains a triage tool. The deployable random-forest selector reaches in-sample R2 0.8203 but leave-species-out RMSE 0.3040; annotation-aware controls improve RMSE to 0.2791 but use target TE/source variables and therefore cannot be used for new-genome deployment.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERPRET-20260630] Binary high-confidence strict-background spikes are not enough for a hidden-TE claim in the current candidate set. The 9 high-score strict-BG fragments have mean binary TE probability 0.8893 but mean SF5 background fraction 0.9974, so they should be treated as binary-detector artifacts or motif triggers until matched-control attribution says otherwise.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERPRET-20260630] Unknown-annotation fragments remain the stronger annotation-audit opportunity: 260 Unknown rows have mean best-main4 SF5 fraction 0.4706, with top cases assigned entirely to DNA/LTR-like main4 probabilities. This supports candidate re-audit, not automatic relabeling.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERPRET-20260630] The requested interpretability literature alignment is not complete yet because the three PDFs are present but the local environment lacks a PDF text extractor. Do not cite paper-derived mechanism details from this branch until the PDF extraction/summarization step is repaired.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERPRET-20260630] Matched-control sanity checks further weaken automatic Unknown relabeling. High-score strict-BG candidates can be matched within honeybee `GroupUn` with acceptable composition deltas, but remain only n=9 false-positive trigger candidates. Unknown-main4-like candidates fail GC matching against human known-main4 controls (case mean GC 0.6638 vs control 0.3920; mean absolute GC delta 0.2723), so treat this as high-GC/SVA/model-bias audit rather than annotation correction.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERPRET-20260630] PDF extraction is now available via user-site `pypdf`; keyword-level method scoping confirms `1703.01365v2.pdf` and `2009.07896v1.pdf` are relevant to attribution/saliency/occlusion-style follow-up. Model-level saliency/occlusion itself remains pending.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERPRET-20260630] Fragment-level occlusion smoke confirms the strict-BG signal is not a 512 bp hidden-TE motif: original binary mean is only 0.0205 for high-score strict-BG cases under fragment context and SF5 main4 remains 0. Unknown-main4-like cases have strong SF5 occlusion sensitivity (mean delta 0.3028), but because matched controls failed GC matching, this supports high-GC/SVA-like/model-bias audit rather than automatic relabeling.
- 2026-06-30 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260630_V2] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison from a fresh species-deduplicated manifest. The report is byte-identical to `SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260630` for `summary.tsv` and `qc_flags.tsv`: 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe concordance classes; six ready plant species still lack strict comparator files (`setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, `green_foxtail`). `02_ready_by_design` remains clean with 803 symlinks and 0 broken links; root-level `RM_*` directories are absent, and quarantined `RM_*` scratch copies are cleanup-safe if no forensic/debug need remains.
- 2026-06-30 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260630_V3] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison again via `srun` job `9854803`. The new report is byte-identical to `V2` for `summary.tsv` (`sha256=c030a6e165bafa963ea22b23b4034cc2e2c6fa14db57ac517cc0412858064b05`): 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe; missing strict comparators remain `setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, and `green_foxtail`. Current report path: `reports/repeatmasker_dfam/SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260630_V3`.

## Engineering Findings
- 2026-08-11 [SF-DIRECT-BASELINE-SCREEN-20260811-R2] Python `csv.DictReader` retains a default 131072-byte field limit. The S0 CPU DATA stage failed before materialization when the frozen chunk manifest contained a larger field; large manifest readers need an explicitly bounded, audited `csv.field_size_limit` plus a regression fixture before resubmission.
- 2026-08-11 [SF-DIRECT-BASELINE-SCREEN-20260811-R2] The frozen RepeatMasker annotation universe is not identical to the Dfam 3.9 name universe: generic/ambiguous names and custom `DR...` families prevent an all-record one-to-one accession/consensus mapping. A leakage-safe S0 must either bind those families to frozen consensus-sequence identities or formally revise the grouping contract; dropping unresolved positives or random/chromosome fallback would bias evaluation.
- 2026-08-11 [SF-DIRECT-BASELINE-SCREEN-20260811-R2] A local 2,000,000-character CSV field limit with `try/finally` restoration fixed the real 1,203,362-character manifest field and passed a 495×17 frozen-source probe; Job `11523252` progressed beyond parsing, confirming the repair rather than repeating the first failure.
- 2026-08-11 [BENCH-5TOOL-DENOMINATOR-CLOSURE-20260811-R2] Exact containers/assets still require CLI-true launch probes: RepeatMasker does not support `-version`; Earl Grey's internal FamDB discovery differs from direct `famdb.py -i`; HiTE's usable entrypoint disappears under `bash -lc`; EDTA's TIR-Learner can fail after dependency checks. These are invalid runtime integrations, not scientific/tool negatives.
- 2026-06-16 [repeatmasker_dfam_species_animals_chr_20260615] Human self-run RepeatMasker+Dfam merged `.out.gz` is incomplete: `chr12` and `chr19` chunk directories have `.cat.gz` and `COMPLETE` but no final `.out.gz`/`.gff.gz`; logs show `ProcessRepeats` failed on missing `Matrices/crossmatch/18p35g.matrix`, and merge used 22/24 inputs.
- 2026-06-16 [repeatmasker_dfam_human_chr12_chr19_repair_20260616] Human Label-A incomplete-merge issue is repaired: chr12/chr19 reruns completed, current human merge uses 24/24 inputs, and `audit_repeatmasker_chunk_outputs.py` now checks final `.out/.gff/.tbl/.masked` files instead of trusting `COMPLETE` alone.
- 2026-06-18 [PIPE-TEFM-SUPP-20260617] NTv3 and Evo2 local checkpoints are not currently runnable through the local HuggingFace fine-tune adapter: NTv3 lacks the required `InstaDeepAI/ntv3_base_model` remote-code module; Evo2 lacks compatible `model_type`/tokenizer adapter metadata. Treat as environment/checkpoint blockers, not performance evidence.
- 2026-06-18 [PIPE-TEFM-SEG-SF-20260618] Slurm command-array TSVs may contain CRLF line endings; `run_cmd_array.sh` must strip carriage returns before `eval`, otherwise argparse may see invalid final arguments such as `base\r`.
- 2026-06-18 [PIPE-TEFM-SEG-SF-20260618] GENERanno 4096 superfamily training OOMs on 12 GB GPUs even with gradient checkpointing; use a high-memory GPU such as A100 80 GB for this branch.
- 2026-06-18 [PIPE-TEFM-SEG-SF-20260618] The current `te_benchmark` PyTorch build does not support Blackwell sm_120 GPUs; avoid gpu051/Blackwell for PyTorch embedding jobs or force CPU for non-model C1 branches.
- 2026-06-19 [RMDFAM_FULLPARTITIONS_RERUN_20260617_STRICTTE_REPAIR] RepeatMasker+Dfam `-species` can generate technically complete but biologically unusable Label-A outputs when specieslibs are too narrow. Plant species were repaired with `-species Viridiplantae`, red_flour_beetle with `-species Coleoptera`, and western_honey_bee with an Apis uncurated Dfam custom library; final base-run QC is 30/30 species OK for chunk/merge completeness and strict TE.
- 2026-06-19 [PIPE-TEFM-REPAIR-20260618] The repair pipeline completed cleanly through Slurm: all train/segment/embedding/eval arrays exited `0:0`, summary TSVs had finite numeric metrics, and non-empty `.err` logs were progress bars or expected model-load warnings rather than failures.
- 2026-06-19 [READY_BY_DESIGN_SELF_LABELA_REPOINT] `02_ready_by_design/**/annotations/self_labelA/**` still pointed to mixed older run batches after the full-partition rerun. All 424 self Label-A symlinks and 18 per-design/aggregate manifest files were repointed to `raw_runs/self_labelA/RMDFAM_FULLPARTITIONS_RERUN_20260617`, with 0 missing targets and 0 broken ready-by-design symlinks. Root-level RepeatMasker scratch directories `RM_*`, including `RM_892559.ThuJun180438082026`, were moved non-destructively to `software_outputs/repeatmasker_dfam/99_internal/rm_scratch_quarantine_20260619/`.
- 2026-06-19 [PIPE-TEFM-LOCK-20260619] Final validation run completed cleanly: prep/train/eval/segment/embedding/summary jobs exited `0:0`, summary completeness is recovery 8 rows, SF5 2 rows, segment 60 rows, embedding 6 rows, and final logs have no `Traceback`, OOM, killed, missing module, or missing file signatures. Segment `nan` values occur only in `median_boundary_error_bp` for stress cases without matched boundaries or true segments.
- 2026-06-21 [PIPE-TEFM-EXTEND-20260620] Final supplement screen completed cleanly after eval/segment resource retries: summary completeness is transfer 81 rows, embedding 20 rows, SF5 1 row, segment 72 rows; final eval/segment/formula/summary jobs exited `0:0`, and final logs have no `Traceback`, OOM, killed process, failed-job, or NaN/loss signatures.
- 2026-06-21 [PIPE-TEFM-CALIB-20260621] Calibration supplement completed cleanly: 9 prep tasks, embedding extract, 5 train tasks, 98 eval tasks, 4 embedding-cluster tasks, formula, and summary all exited `0:0`; 98/98 expected eval JSONs exist; summary has 98 eval rows and 4 embedding rows; final logs have no failure signatures. Eval array throttle was safely raised from `%4` to `%8` then `%12` after checking idle shared-gpu capacity.
- 2026-06-21 [PIPE-TEFM-ANCHOR-20260621] Anchor supplement completed cleanly after one repaired SF5 loader failure: initial SF5 job hit the Transformers/PyTorch `torch.load` safety gate, `diagnose_fragments.py` was fixed to initialize from config and load local state dicts explicitly, and CPU-bigmem retry `9298759` completed. The 80GB training job was already healthy and completed in 2:58:14, so only safe race paths were used for SF5/eval; redundant eval race tasks were cancelled after the main eval finished.
- 2026-06-29 [PIPE-TEFM-FINAL-20260623] NTv3 uses single-base tokenization for this adapter; k-mer/offset label compression is invalid and causes shape mismatches. Use `token_label_mode=ntv3_single` for NTv3 train/eval rows.
- 2026-06-29 [PIPE-TEFM-FINAL-20260623] NTv3 checkpoint reload needs rotary cache filtering: `model_state.pt` may contain regenerated `*.rotary_embedding.cos_cached` and `*.rotary_embedding.sin_cached` buffers. Filter only those suffixes before strict `load_state_dict()`; failed eval jobs `9844255`/`9844256` are runtime failures superseded by `9845158`/`9845159`.
- 2026-06-30 [PIPE-TEFM-FINAL-STRICTSEG-20260629] Strict bp/segment inference must mirror training token geometry. NTv3 strict inference failed with a 1024/1025 rotary mismatch until it used `ntv3_single` max_length=`window`; NTv2 `EsmTokenizer` lacks `return_offsets_mapping`, so k-mer token probabilities must be expanded back to bp spans from `tokenizer.tokenize(seq)` after the leading special token. Earlier strict NTv2 outputs before retry `9852364` are superseded.
- 2026-06-30 [DENOVO_B_ANIMAL_EVAL_RM_SCRATCH] Root-level `RM_*` directories have reappeared under `/srv/beegfs/scratch/shares/ds4dh/common/ab-initio-TE` during the active de novo+Dfam RepeatMasker overlay, with 28 directories observed at 09:51 CEST. They contain RepeatMasker scratch/batch files for currently running species such as cattle, lizard, opossum, pig, honeybee, beetle, horse, and X. laevis. Do not delete or quarantine these root-level `RM_*` directories until Slurm jobs `9843979`, `9854672`, and dependent compare jobs have completed and their final outputs are verified.
- 2026-06-30 [DENOVO_B_ANIMAL_EVAL_STRUCTURE] The current de novo benchmark annotations are not allocated into `repeatmasker_dfam/02_ready_by_design`; they live under `software_outputs/de_novo_benchmark/DENOVO_B_ANIMAL_EVAL_20260620/raw_outputs/<species>/<tool>/` for base de novo outputs and `.../dfam_augmented/<species>/<tool>/` for de novo-library + Dfam overlay outputs. As of 10:28 CEST the base and Dfam UCSC comparison jobs (`9854671`, `9854673`) are submitted but still dependency-pending, so `ucsc_compare_full_20260630/` and `dfam_augmented/ucsc_compare_full_20260630/` are not final evidence yet. Root-level `RM_*` directories updated at 10:28 CEST remain active RepeatMasker scratch and should not be cleaned until these jobs complete and the comparison reports are verified.

---

## Research Findings

- 2026-06-30 [PIPE-TEFM-FINAL-GENOMEDECAY-20260630] Deployable anchor selector benefits from target-genome k-mer shift: bounded k=4 k-mer distance to anchor prototypes reduced leave-species-out RMSE from `0.3042` to `0.2666`. Assembly stats alone worsened RMSE to `0.3441`, so N50/genome size should be treated as weak/noisy auxiliary variables until validated with larger panels and better sampling.
- 2026-06-30 [PIPE-TEFM-FINAL-GENOMEDECAY-20260630] Mash/sourmash were not installed in this environment. The current k-mer selector addendum is screen-grade only; claim-grade selector work should install/version Mash or sourmash, compute genome-wide signatures, and use leave-clade-out validation.
- 2026-06-30 [PIPE-TEFM-FINAL-GENOMEDECAY-20260630] Fragmentation is no longer a threshold-sweep problem. 3/3 council reviewers attribute the strict segment/boundary gap mainly to bp-level token objective lacking interval/boundary structure. Next route should be frozen interval refiner or boundary-aware head; double-strand prediction is only a cheap sanity check.
- 2026-06-30 [PIPE-TEFM-FINAL-GENOMEDECAY-20260630] Any claimed fragment reduction must report missed_true_rate, pred_true_backed_rate, short_true_backed_rate, deleted true-backed vs false-positive fragment rate, and length/nested-TE stratification. Fewer predicted segments alone is not evidence of better annotation.
- 2026-06-30 [PIPE-TEFM-FINAL-FRAGSANITY-20260630] Bounded mouse chr1 forward/RC sanity supports conservative two-strand consensus, not permissive union: `consensus_min + crf_style_penalty4` improved segment-F1@IoU0.8/boundary5 from forward CRF `0.3569` to `0.4149`, while `max_prob + crf` dropped to `0.3081`.
- 2026-06-30 [PIPE-TEFM-FINAL-FRAGSANITY-20260630] Truth-aware oracle fill of supported true intervals reached segment-F1/boundary-F1 `0.9711` on the bounded mouse chr1 screen. This means current bp logits often touch true intervals, so the strongest next component is a learned interval refiner that approximates oracle keep/merge/refine decisions without truth labels.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERVALREFINER-20260630] A deployable lightweight frozen-bp interval refiner did not beat consensus+CRF in the 40-window mouse chr1 smoke: `consensus_min_crf` segment-F1@IoU0.8/boundary5 was `0.4685`, while the best refiner variant was `0.4667`. The truth-aware oracle remained near-perfect (`0.9919` segment-F1/boundary-F1), so the gap is structural interval/boundary learning rather than lack of post-hoc threshold/gap tuning.
- 2026-06-30 [PIPE-TEFM-NEXT-DECAY-FRAG-20260630] The current genome-derived selector is not usable as an exact new-species trust formula: best point row `baseline_plus_kmer / leave_species_out` has RMSE `0.2642`, top-1 anchor accuracy `0.4545`, top-2 accuracy `0.6818`, and leave-clade-out RMSE around `0.40-0.42`. Do not use it to claim reliable cross-clade F1 prediction.
- 2026-06-30 [PIPE-TEFM-NEXT-DECAY-FRAG-20260630] A conservative selector action policy is screen-usable only as top-2 anchor routing plus local chromosome probe: action contains true best/top2 anchor in `0.8636` of species and mean regret after action is `0.0071`, but high-confidence single-anchor coverage is `0.0`.
- 2026-06-30 [PIPE-TEFM-NEXT-DECAY-FRAG-20260630] Frozen-logit trainable fragmentation components failed in bounded smoke: post-hoc `consensus_min_crf_posthoc` segment-F1@IoU0.8/boundary5 was `0.4685`, while boundary CNN was `0.2778`, duration prior `0.2366`, and linear CRF `0.1798`. Trainable CRF/boundary heads should only be retried with better emissions/backbone embeddings or richer interval proposals.
- 2026-06-30 [PIPE-TEFM-NEXT-DECAY-FRAG-20260630] Duration-aware suppression alone is unsafe for TE interval usability: it reduces predicted segments but raises missed_true_rate to `0.0484` and deletes true-backed signal, so any future semi-Markov/duration decoder must retain missed_true_rate, pred_true_backed_rate, short_true_backed_rate, and deleted true-backed fragment audits.
- 2026-06-30 [PIPE-TEFM-STRUCTDEC-20260630] Jointly trained structured decoder backends had not been tested before; this smoke attaches HMM/CRF/semi-Markov-style losses to GENERanno logits during fine-tuning. The route shows signal: test segment-F1@IoU0.8/boundary5 improves from CE baseline `0.3069` to HMM `0.3836`, CRF `0.3631`, and semi-Markov proxy `0.4258`.
- 2026-06-30 [PIPE-TEFM-STRUCTDEC-20260630] Joint structured decoding is promising but not solved. The best semi-Markov proxy improves segment/boundary-F1 but raises missed_true_rate to `0.3033`; joint CRF has better missed_true_rate `0.1721` but weak boundary-F1 `0.0921`. Next attempt needs explicit boundary loss plus true-retention penalty before scaling.
- 2026-06-30 [PIPE-TEFM-PURSUE-DECAY-STRUCT-20260630] The deployable generalization selector is usable only as a conservative router, not a point F1 formula: in-panel leave-species top-2+local-probe reaches contains-best `0.8636` and mean regret `0.0071`, while leave-clade must abstain because the best top-2 formula contains-best is only `0.6364`.
- 2026-06-30 [PIPE-TEFM-PURSUE-STRUCTDEC-20260630] Boundary/retention joint training improves strict segment and boundary metrics but still fails the true-retention gate. `semimarkov_retention` improves segment-F1 `0.3069` -> `0.4439` and boundary-F1 `0.1414` -> `0.2290`, but missed_true_rate rises `0.2623` -> `0.3525` and about half of deleted CE fragments are true-backed, so this variant cannot be scaled.
- 2026-08-11 [FRAG-EVIDENCE-REGISTRY-20260811-R2] HMM2 is only a historical fixed comparator; no current accepted production postprocessor is frozen. Gap-based MERGE_STRICT/LOOSE projections are comparator-only and do not constitute the immutable-leaf, richer-evidence typed-parent lattice required to re-enter DEC-001/002. The F scientific route therefore remains blocked even though the 6/6 asset integrity audit passes.
- 2026-06-30 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260630_V4] Re-ran current ready-by-design self Label-A vs UCSC/local strict-TE comparison via `srun -p public-short-cpu`. The report is byte-identical to `V3` for `summary.tsv` and `qc_flags.tsv`: 24 paired entries, 7 high, 2 moderate, 5 low, 10 severe; missing strict comparators remain `setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, and `green_foxtail`. This confirms the latest ready-by-design RepeatMasker+Dfam vs UCSC audit is stable, while the separate de novo benchmark UCSC comparisons are still pending on EarlGrey/Dfam dependencies.
- 2026-06-30 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260630_V5_2] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison again with `srun -p public-short-cpu -c 4 --mem=16G`. The report remains byte-identical to `V4` for `summary.tsv` and `qc_flags.tsv` (`summary.tsv` sha256 `c030a6e165bafa963ea22b23b4034cc2e2c6fa14db57ac517cc0412858064b05`; `qc_flags.tsv` sha256 `9073736e137971f5535838470d098b98df9005061d09f81c6afdbab6e0a8fef3`): 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe. The ready-by-design self Label-A/UCSC audit is stable; the separate de novo benchmark UCSC comparisons are still pending.
- 2026-07-01 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260701_SINGLE] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison from a fresh species-deduplicated manifest. The completed single-process report is byte-identical to `20260630_V5_2` for `summary.tsv` and `qc_flags.tsv` (`c030a6e165bafa963ea22b23b4034cc2e2c6fa14db57ac517cc0412858064b05` and `9073736e137971f5535838470d098b98df9005061d09f81c6afdbab6e0a8fef3`): 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe; six ready species still lack strict comparator files. The ready-by-design allocation remains valid with 803 symlinks and 0 broken links; root-level `RM_*` directories are absent, and quarantined `RM_*` scratch copies such as `RM_892559.ThuJun180438082026` are not final annotation products.
- 2026-07-03 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260703_200218_FRESH] Fresh `srun -p public-interactive-cpu` rerun of current ready-by-design self-run RepeatMasker+Dfam Label-A vs UCSC/local strict-TE comparators completed from a newly generated species-deduplicated pair manifest. The report is byte-identical to `SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260703_SINGLE` for `summary.tsv` and `qc_flags.tsv`: 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe; missing strict comparators remain `setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, and `green_foxtail`. Current `02_ready_by_design` has 803 symlinks, 424 self Label-A links, 273 UCSC comparator links, 106 genome links, and 0 broken links.

## Engineering Findings

- 2026-08-11 [BENCH-5TOOL-SMOKE-20260811-R1] Digest-pinned containers are necessary but not sufficient runtime evidence. The CPU-only five-workflow smoke completed in 913 seconds with 0 GPU and a semantically valid 5-cell matrix, yet none cleared every identity/database/min-input/adapter gate: Dfam/FamDB configuration blocks RM2+RM and Earl Grey, exact HiTE 3.3.3 lacks an accepted local SIF, EDTA prints only `v2.3` so patch identity remains unprovable, and TEtrimmer 1.7.4 source-overlay execution still lacks a canonical interval output/Pfam closure.
- 2026-08-11 [FRAG-EVIDENCE-REGISTRY-20260811-R2] In the Baobab `benchmark_core` bootstrap, enabling Bash nounset before `conda activate` can fail inside activation hooks on optional variables such as `MKL_INTERFACE_LAYER`. Job `11521393` exposed this in 1 second before payload. The reviewed pattern is `set -eo pipefail` -> source fixed Conda bootstrap -> activate environment -> `set -u`, with a pre-payload trap that records `FAILED_ENV` without overwriting the scientific state machine. Retry `11521479` then completed in 14 seconds with 0 GPU.
- 2026-08-12 [SF-DFAM-P3-IDENTITY-RECOVERY-SHARDED-20260812-R2] Four read-only workers with 35 atomic units completed the 321,856-object partition-3 scan in 1:40:52 versus the serial 4.41h projection. Two-level unit manifests and immutable pointer-last state publication allowed exact post-run closure without treating partial checkpoints as evidence.
- 2026-06-30 [PIPE-TEFM-FINAL-FRAGSANITY-20260630] Full 1200-window mouse chr1 forward/RC sanity inference completed to 1200/1200 windows, but the prototype full-length Python metric loop was too slow and job `9856510` was cancelled before writing TSV/JSON. Do not treat `mouse_chr1_full` as evidence; optimize metrics before rerunning full-chromosome RC/oracle screens.
- 2026-06-30 [PIPE-TEFM-FINAL-INTERVALREFINER-20260630] The first 120-window interval-refiner attempts (`9856920`, `9856939`, `9856942`) were cancelled after inference without TSV/JSON evidence. The accepted smoke (`9856944`) completed at 40 windows after adding stage logs, cheap threshold-selection proxy, and RF candidate subsampling. Larger interval-refiner screens need optimized metrics/proposal construction before submission.
- 2026-06-30 [PIPE-TEFM-NEXT-DECAY-FRAG-20260630] The bounded trainable decoder job `9858072` completed cleanly in `00:01:34` with exit code `0:0`; status JSON reports `ok=true`, `n_windows=40`, and finite TSV metrics. The failure is methodological, not an execution failure.
- 2026-06-30 [PIPE-TEFM-STRUCTDEC-20260630] GENERanno remote code requires Python 3.10+ typing support. First job `9860192` failed under compute-node default Python 3.9; retry `9860193` succeeded after explicitly using `/home/users/j/jwang/.conda/envs/te_benchmark/bin/python` and completed in `00:10:32`.
- 2026-06-30 [PIPE-TEFM-PURSUE-STRUCTDEC-20260630] PreToolUse review hook can misidentify direct local smoke commands that reference an old `--init-model-dir` as submissions for that old exp. For this route, use exp-scoped `pre_submit_gate.py` plus Slurm sbatch for real screen jobs rather than bypassing code-review discipline.
- 2026-06-30 [PIPE-TEFM-PURSUE-MINHASH-INTERVALSURV-20260630] Genome-wide bottom-k MinHash-equivalent distance improves leave-clade selector diagnostics but does not make the formula deployable: the selected in-panel policy remains `baseline_plus_kmer` top-2/local-probe, while leave-clade/new-clade must abstain.
- 2026-06-30 [PIPE-TEFM-PURSUE-MINHASH-INTERVALSURV-20260630] Interval-survival decoder is a real positive signal but not a usable fragmentation solution: segment-F1 and boundary-F1 improve while missed_true_rate stays within the `+0.03` gate, yet deleted true-backed fraction remains high (`0.4592`), so fragment suppression is still not sufficiently selective.
- 2026-06-30 [PIPE-TEFM-PURSUE-RETCONSTR-20260630] Final retention-constrained decoder exposes the tradeoff limit of this bounded structured-decoder family: true retention improves (`missed_true_rate` 0.2541 vs CE 0.2623; deleted_true_backed_fraction 0.2727 vs 0.4592 prior), but strict segment/boundary fall below CE. Stop this decoder line for the milestone and report as future work.
- 2026-07-01 [PIPE-TEFM-CAP-FRAGARCH-20260701] Lightweight interval heads on frozen GENERanno embeddings are insufficient as implemented. `boundary_proposal` and `anchor_free_interval` do not beat CRF-style smoothing on strict segment-F1@IoU0.8 and boundary-F1@5bp, and both retain high true-backed deletion rates; a second capability round would need stronger proposal matching/set prediction or richer coordinate-aware training, not head-level threshold tuning.
- 2026-07-01 [PIPE-TEFM-CAP-FRAGARCH-20260701] Smoothing remains a strong but biologically unsafe comparator. CRF-style smoothing greatly improves strict segment/boundary metrics over CE raw on human/mouse quick panels, but deleted_true_backed_fraction is high (`0.8518` human, `0.4889` mouse), so it cannot be treated as solved interval annotation.
- 2026-07-01 [PIPE-TEFM-CAP-FRAGGRAPH-20260701] Fragment graph linking as implemented separates the two failure modes: preservation-first `keepall` keeps true-backed fragments but learns no useful links and equals CE raw; learned `keepdrop` can beat CRF-style smoothing on human strict segment/boundary (`0.4964`/`0.2458`) but again deletes true-backed fragments (`0.8632`) and does not transfer to mouse. The next method, if any, must learn links without dropping CE fragments.
- 2026-07-01 [PIPE-TEFM-CAP-FRAGGRAPH-20260701] Capability-pursue closeout: after Round 1 frozen interval heads and Round 2 fragment graph linking, the current interval-aware fragmentation module is not publishable as a positive capability. The publishable use is a rigorous limitation/negative result: bp-level TE detection is strong, but strict interval reconstruction remains unsolved without richer global interval modeling and true-backed retention constraints.
- 2026-07-01 [PIPE-TEFM-CAP-POSTPROC-20260701] Multi-threshold postprocess auditing confirms the threshold is part of the fragmentation tradeoff but not the main solution. Human strict-safe best shifts from `raw_t0.50` segment-F1/boundary-F1 `0.1542`/`0.0763` to `raw_t0.20` `0.2422`/`0.1143` without true-backed deletion, while mouse strict-safe best is `gap25_min40_t0.50` `0.4589`/`0.1575`. However, best observed HMM/length-adaptive rows improve interval metrics mainly by deleting true-backed fragments, so no universal threshold/HMM recipe is promoted.
- 2026-07-01 [PIPE-TEFM-CAP-POSTPROC-20260701] Length-adaptive short-raw/long-HMM is not sufficient in the bounded human/mouse screen. It can preserve some short calls, but long TE split/fragment counts remain high or worsen, and human best length-adaptive segment rows have deleted_true_backed_fraction around `0.86`. Use this as support for reporting threshold sensitivity and biological annotation fragmentation, not as a restarted post-hoc optimization route.
- 2026-07-03 [SELF_LABELA_VS_UCSC_CURRENT_READY_RERUN_20260703_SINGLE] Re-ran the current ready-by-design self Label-A vs UCSC/local strict-TE comparison from `02_ready_by_design/manifests/MANIFEST_ALL.tsv` after the latest annotation/directory audit. The report is byte-identical to the stable `RERUN19` summary: 24 paired entries, 7 high, 2 moderate, 5 low, and 10 severe concordance classes; six PlantTE/reserve species still lack usable UCSC/local strict comparators (`setaria_italica`, `tomato`, `wild_rice`, `arabidopsis_lyrata`, `grape`, `green_foxtail`). Current ready-view entries remain allocated by design/role and all 106 self Label-A manifest rows resolve to `RMDFAM_FULLPARTITIONS_RERUN_20260617`.
- 2026-07-03 [RMDFAM_ARTIFACT_AUDIT] The current RepeatMasker+Dfam usable entrypoint is `software_outputs/repeatmasker_dfam/02_ready_by_design`, with raw self-run outputs in `raw_runs/self_labelA` and UCSC/local comparators in `comparators/ucsc_reference_repeatmasker`. Quarantined `RM_*` scratch directories under `99_internal/rm_scratch_quarantine_20260619` are cleanup-safe if no forensic inspection is needed; `RM_892559.ThuJun180438082026` is only about 60K. The large `99_internal` footprint is Dfam partition cache (`dfam_partition_downloads`, about 429G), not RepeatMasker scratch.

## How to use
> 每条格式：`- <YYYY-MM-DD> [<exp_id>] <一句发现>`。上面的 `---` / 本段是 SessionStart hook 统计 Engineering findings 的区间右边界，**勿删**。
### 2026-08-11 — Dfam FamDB partitions do not all expose a name index

- **Engineering**: Dfam 3.9 full partition `dfam39_full.3.h5` has `Lookup/ByStage` and `Lookup/ByTaxon` but no `Lookup/ByName`. Calling the leaf API `get_family_by_name()` directly therefore raises `KeyError`; the top-level official `FamDB.get_family_by_name()` wrapper intentionally continues past partition-local failures.
- **Boundary**: do not broadly catch all errors in a provenance audit. A future resolver may skip only a verified structurally absent name index and must still fail on unreadable/corrupt objects. The failed Job `11523938` produced no identity or scientific evidence.

### 2026-08-11 — RM denominator validity closed; HiTE demo exceeds a 600-second minimum budget

- **Engineering**: exact RepeatModeler 2.0.9 + RepeatMasker 4.2.4 + Dfam 4.0 completed on the frozen fixture and produced 43 canonical adapter rows. The earlier RM `-version` harness failure was not a tool failure.
- **Engineering**: exact HiTE 3.3.3 direct argv and help identity are valid, but the official demo was still in step 3.3 at 600 seconds and produced no final GFF. Treat this as timeout/incomplete execution, never as foundational block or biological performance evidence.
- **Engineering follow-up (2026-08-12)**: the single preregistered isolated continuation completed in 21m58.53s command time and produced 14,315 canonical rows. Thus 600 seconds was an under-budgeted smoke timeout, not a HiTE runtime incompatibility. The evidence remains cross-run and claim-ineligible; it does not retroactively make the parent aggregate successful.

### 2026-08-11 — Exact Dfam provenance does not cover the frozen S0 P-state universe

- **Research**: the complete CPU audit resolved only 6,447/6,727 P identifiers uniquely under exact Dfam name/accession semantics. The 279 missing identifiers include very frequent mammalian families (`L2a`, `L2c`, `L2b`, `L1MB1`, `L1M5`), so dropping them would materially change the biological task and cannot be treated as negligible cleanup.
- **Research**: the frozen S0 labeler explicitly excludes 43,728 potential-TE records across 10 identifiers (mainly SVA and L1-dependent retroposons). They were retained in a separate audit with exact conservation, proving that direct-superfamily data construction requires a deliberate human-gated label/exclusion policy rather than an implicit fallback.
- **Engineering**: Dfam partition-layout repair is valid: partition 3 is the only frozen partition without `Lookup/ByName`; the retry completed 35.6 million-record enumeration with zero conservation error and no broad exception swallowing.
- **Research decision**: 3/3 external reviewers independently agree that this valid negative is a comparability blocker, not permission to shrink the denominator or train. They disagree on alias-first versus homology-first repair, so that choice is retained as an explicit human gate rather than resolved by implementation convenience.

### 2026-08-12 — Partition-3 exact metadata traversal is I/O-bound and exceeds a 2-hour serial contract

- **Engineering**: Job `11525316` passed 13/13 allocation-side tests and made steady progress without traceback, but serial exact attr traversal reached only 30,000/321,856 datasets in about 1,480 seconds. The projected exhaustive runtime was about 4.41 hours while CPU/RSS stayed low, so the controller cancelled early instead of consuming the full 2-hour allocation for a certain timeout.
- **Boundary**: 30,000-dataset zero-hit telemetry is only 9.321096% coverage and is not identity evidence. Any repair must retain exhaustive exact-case semantics and full dataset/consensus/model counts; use deterministic disjoint shards or a separately reviewed longer CPU walltime, never sampling or partial-result promotion.
- **Pre-submit resource finding**: the proposed 20-minute/4-worker throughput preflight is deterministically infeasible before any allocation: `15,878 s / 4 = 3,969.5 s`, while 25% headroom permits only 900 s of work. Independent code review therefore blocked submission and saved a known-negative billed allocation. A defensible formal retry needs a 3-hour envelope (8,100 s work budget at 25% headroom, requiring only about 1.96× speedup) plus exact per-unit resumability; 2 hours would require an unmeasured ≥2.94× sustained BeeGFS speedup.
- **Cross-node filesystem identity finding**: Job `11526687` demonstrated that `st_dev` is not a stable binding field across Baobab login/compute mount namespaces: the same frozen BeeGFS object had device 42 on login and 65 on `gpu034`, while symlink-target hash, inode, size, mtime and mode matched. Cross-node asset guards must treat device ID and raw absolute path as audit observations, bind only an explicit alias-normalized logical path plus stable file/HDF5/version pins, and revalidate before/after use. This does not justify weakening any scientific denominator or exact-match rule.

### 2026-08-12 — Partition-3 exact-name recovery is exhaustively negative

- **Research**: Job `11526905` exhaustively scanned all 321,856 unique Dfam 3.9 partition-3 Families objects and found zero case-sensitive exact-name candidate rows for the frozen 279 unresolved identifiers (6,432,583 occurrences). This closes the partition-3 exact-name hypothesis but does not establish biological absence or absence from other official alias/accession sources.
- **Comparability**: the missing mass is too large to drop as cleanup. Any direct-S0 continuation must retain the denominator and labels, replace the identity-source component under a human gate, and use homology only for split grouping. Prefix/case/copy-derived fallbacks remain prohibited.
## Research finding — Dfam 3.9 post-hoc identity recovery is insufficient for current direct-S0 data

Final Job `11528267` scanned 26,279 DF and 4,095,118 DR records with complete official alias grammar. Only the curated 57 candidate rows exist: 50 unique identities, two ambiguous identifiers and 227 missing. Raw DR contributes zero exact support. This is a source-route valid-negative, not evidence against direct-superfamily prediction. The current dataset cannot support leakage-safe S0 because 73.229% occurrence mass lacks authoritative identity; a new annotation-time accession-preserving benchmark is the only proposed re-entry.

### 2026-08-12 — Slurm walltime identity must use scheduler facts, not an optional environment string

- **Engineering**: accession roundtrip Job `11528744` had authoritative `scontrol` resources exactly matching the reviewed 1CPU/4GiB/20m/0GPU contract, but failed before payload because `SLURM_TIMELIMIT` was absent or not represented in the runner's allowlist. Baobab resource guards must reconcile numeric JobId against strict `scontrol show job -o` fields and the exact submission command, then revalidate before canonical publication; an optional environment variable is not a sufficient authority.

### 2026-08-12 — FamDB aggregate APIs cannot assume leaf bookkeeping attributes

- **Engineering**: accession roundtrip repair Job `11528885` proved the scheduler guard repair but failed before RepeatMasker because the installed `FamDBLeaf` has no `added` attribute. Any future export component must first prove the exact installed FamDB constructor/leaf API against the six frozen accessions in an isolated contract; do not infer aggregate bookkeeping fields from another FamDB version.

### 2026-08-12 — FamDB read-mode cleanup must not invoke the write finalizer

- **Engineering**: isolated leaf probe Job `11533175` reached the single in-memory 72-call probe, then failed because `FamDB.finalize()` calls `FamDBLeaf.finalize()`, whose `added` bookkeeping exists only for write/update mode in this installed API. Read-only consumers must close underlying HDF5 handles through the installed read lifecycle and must not call a write finalizer.
- **Closure evidence**: final repair Job `11534847` froze all 72 exact-access observations before cleanup and then closed exactly 12 unique `leaf.file` handles once each; 6/6 frozen accessions resolved with no fallback and no identity drift. This validates the leaf API/read lifecycle component only, not annotation, homology-safe data, or direct-superfamily performance.
- **Adapter evidence**: Job `11535362` shows that the same six records can be rendered as canonical-name and accession.version FASTA views while preserving byte-identical ordered sequences and raw classes; the empty-name DR row safely falls back to its unversioned accession. This validates header/manifest syntax only and must not be called RepeatMasker compatibility or representative annotation concordance.
- **Evidence boundary**: because cleanup failed before the returned observations were frozen, exact-access results remain unknown. Control-flow position is not evidence; never reconstruct PASS or missing/duplicate counts from the fact that the probe function returned in memory.

### 2026-08-12 — Reviewed runtime closure must include shared gate scripts

- **Engineering**: F Job `11529694` failed closed before any Rice input because `scripts/pre_submit_gate.py` was frozen as runtime code but omitted from machine `reviewed_files`. Installing a PASS gate requires equality between the config runtime set and the independently reviewed hash set, including shared infrastructure; login-side `pre_submit_gate.py` alone does not prove that closure.

### 2026-08-12 — Consensus-coordinate evidence is real but insufficient for Rice T1 parent assembly

- **Research**: Job `11531090` shows the frozen consensus-coordinate/strand signal is not noise: exact-group recovery `0.1389` versus shuffle `0.0013`, and pairwise harmonic `0.3082` versus `0.00054`. This supports the narrow mechanism premise that consensus collinearity contains copy-structure information.
- **Research limitation**: the same candidate maps only `55.51%` of leaves, has cross-RM-ID false fusion `7.59%`, and is decisively worse than GAP100 on exact recovery (`0.1389` vs `0.3717`), pairwise harmonic (`0.3082` vs `0.6691`) and topology (`0.1053` vs `0.4737`). All paired bootstrap intervals for candidate-minus-comparator are below zero.
- **Decision implication**: exact seed/consensus coordinate evidence alone is not a viable preservation-constrained parent assembler under this contract. Do not rescue it by test-family threshold tuning, adding Fly/H0, or reviving gap/HMM/CRF/local-graph cousins. Re-entry would require genuinely richer global evidence and a new pre-registered mechanism.
