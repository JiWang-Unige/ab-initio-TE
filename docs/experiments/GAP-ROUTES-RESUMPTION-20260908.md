# Gap研究恢复：C收尾与剩余路线提案

> 后续2026-09-08用户已明确授予研究范围内持续实验权限，下文“待批准”是当时提案，
> 不再是当前等待事项。旧全量训练47:30:23超过剩余额度，现已选择独立的预算内
> [A/B1配对筛选协议](GAP-A-B1-SCREEN-20260908-R1.md)，不改写旧实验。
> 新screen及后续动作诊断现已完成：HN信息增量阳性、H0-S未过门；HN固定排序未过
> 宽松动作必要条件，停止CAL/部署。当前结论见[路线收尾](GAP-ROUTE-TERMINAL-AUDIT-20260908.md)。

日期：2026-09-08。研究范围以当前对话为准，不以机器goal状态判定。
本文件的新实验设计为待批准提案，不是执行令。不恢复ACTIVE_GOAL.json或退役Skill。

## C：结论已完成，不再等评阅

沿用已完成的原27单元收集、完整性与指标复算，不重复提交或核验。
来源：[完整结果](GAP-BRIDGE-C-UTILITY-20260906-R1-RESULT.md)、
[冻结协议](GAP-BRIDGE-C-UTILITY-20260906-R1.md)。

- M0：TP54/FP37/FN189，F1=0.32335329341317365。
- MW：同M0，delta=0，新增正确链0、丢失0。
- MP：TP54/FP36/FN189，F1=0.32432432432432434，delta=0.0009710309111506943，新增正确链0、丢失0。
- 两臂均不满足原三项合取继续门。MP净少一个FP但产生两个新unmatched身份，不能
  说恢复了基因或只是删去一个错误预测。新增mask与基因区重叠是风险描述，不是实测损伤。

工程完成、数值已验证；本探索性投资门阴性。正式关闭该固定预测器/面板的gene-utility
扩张理由，不继续C、不调门、不启动C衍生partial-fill学习。结论不能外推到所有TE
材料修复：DEV已消费，参考不完整，MW/MP非部署模型、非数学效用上界；MP对比MW同时
改变材料量和分辨率。也不能据此否定尚未训练的A。

## 现有批准中是否还有可直接执行的计算

没有识别到未完成的同协议计算：A获准的stats/8-crop smoke/全母体coverage均已完成，
C获准的readiness/全9-core三臂比较及结果处理已完成；B0诊断已完成。原A配置明确
`full_training_authorized=false`且科学gate未锁定；B1只被列为deferred proposal。
已批准常规步骤继续有效，但不能由此推定新训练、候选资格变化或CAL再次选择获准。

## 建议下一步：A的独立TE信息增量实验，先限DEV，不做部署选择

这是新科学选择：不再以C gene utility为理由，而检验冻结NT在完整P3信息之外是否
改善gap材料比例预测。建议先回答这一较窄问题，暂不投入B1或Gate L。

### 待批准的具体变更

1. **缺失输入策略**：保持NT原生4096/origin0网格和token投影，不加token预算、不移窗。
   所有候选继续保留在总清单中。NT crop不完整者在两臂均不进入配对训练；未来若执行
   calibration/deployment，则这类候选必须显式abstain，保留在覆盖/安全分母中，不填概率。
   已有coverage证据为89/926253：TRAIN74、CAL-FIT4、CAL-GATE11、DEV0，全部known。
   因此新配对训练资格预期741503−74=741429；这改变原训练母体，必须明示批准。
   原7项stats和已准备3项seam stats可原样复用：它们不依赖缺失NT概率；不得偷偷重拟合。
   loss仍采用原六长度层等总权重、层内按长度加权，权重按新训练资格计算并记录。
2. **受控对比**：H0/HN保持144 channels/10 scalars、同初始化与容量；仅HN拥有实际NT
   logit及新增seam输入，H0新增槽标准化后归零。P3/NT均冻结，原候选1–512bp、crop、
   loss、AdamW参数、batch512、两遍block-stream不改；seeds17/42/20260902全部保留。
   不混入训练顺序变更，不选最佳seed，不沿用旧H模型充当新的配对H0。
3. **首阶段只到DEV**：不读CAL科学指标、不拟合校准器、不锁部署阈值、不读chr19–22。
   平均三seed logits形成各臂预测，报告原bp-weighted action AUPRC、Brier及其不可约项/
   fraction-MSE分解、原长度层与homology-purged诊断；所有DEV候选保留，不筛seam尾部。
4. **新资源决策门（待批准，非原协议门）**：DEV bp-weighted fraction-MSE相对H0至少
   降低5%，且action AUPRC不低于H0；同时列出三seed配对差异，不择优汇报。
   5%是沿既有提案的投资门，不是统计显著性或已验证生物学阈值。H0分母为0时门不可判，
   不记PASS。未过则结束该次A信息增量实验，不调参重跑；通过仅支持另拟部署安全评价，
   不自动打开CAL或测试。DEV重复使用导致此结论仍是探索性。

### 实施与资源准备

复用 `scripts/experiments/GAP-BRIDGE-P3-NT-R2/prepare_pair.py` 的配对输入和
`scripts/experiments/GAP-BRIDGE-NEURAL-STAGE1-R1/stage1_train.py` 的既有trainer逻辑，
新实验目录内实现H0/HN和显式coverage资格，不修改旧结果/协议。相关验证只覆盖新增的
资格处理、相同配对流、NT缺失不变成p=0.5以及固定训练参数，不复跑C或既有全母体审计。

已记录A/C消耗3372GPU秒=0.9367GPUh，共享24GPUh尚余约23.0633GPUh；预算不重置。
建议新A先单GPU最多20分钟真实训练吞吐smoke（TRAIN、按原顺序的有界数据、无科学评分），
测量后仅在剩余预算足以覆盖六个head两遍训练及DEV推理时执行完整实验；否则报告
资源不可行并停，不缩为单seed/少遍数。smoke计入预算、不作为最终模型起点。
每job≤12h，单GPU、资源参数依据既有模型环境和实测内存锁定；不在login计算。
新增任何失败先区分工程与预算，保存输出；无自动阈值/seed扫描或扩大额度。
这是申请包含有界smoke及满足实测成本条件后的完整DEV实验的一次性授权，不需要在
正常smoke通过后再询问。原训练母体不完整是已知事实，不以重跑相同推理消除。

## 其他未执行路线及需要的决定

- **B1 ordered/shuffled H replay**：仍可检验训练顺序影响，但现有seam尾部集中不证明
  顺序致因。若选此路线而非A，建议原H/原741503训练母体、原三seed与两遍，每seed配对
  初始化，一臂原顺序、另一臂固定seed的block顺序置换；各block内部坐标顺序不变，
  保持carry语义。只看DEV，报告同A的MSE/AUPRC与训练过程；用同一5%/AUPRC非劣
  投资门作为待批准的新标准。先≤20分钟成本smoke，完整两臂三seed必须容纳在上述
  剩余额度内；不与A同时占用同一余额。此对照不重新宣称旧G/R/H成功。建议暂缓B1，
  不把它记为失败；优先级和新训练批准是尚缺决定。
- **Gate L/empty-site**：无独立truth panel及可用专家资源，维持资源性未执行。
  若要实际尝试，需另行确定同源插入/empty-site成对组装证据、坐标、参考版本及标注
  责任人，先冻结资格与评价；现有FlyBase packets不能替代它。本轮不申请采购/招募或
  外部发送。无这些材料，不能给出可执行实验或承诺科学成败。
- **旧结构decoder、G/R/H、C**：已有路线内停止结论，不因本次取消评阅而复跑。

## 本次一次性待决事项

建议批准上述A的“明确coverage资格变化 + 三seed配对DEV实验 + 新投资门 + 原剩余预算
内有界smoke/完整运行”的组合；B1和Gate L继续记为未执行。若目标要求两者也必须
实际实验，需要分别选择B1的资源优先级以及Gate L真实panel来源，而非把它们行政性
归为失败。未完成所有路线的事实保持不变。
