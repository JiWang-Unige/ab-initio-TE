# 跨物种TE掩码模型：自主接续范围与完成标准

## 2026-09-08 当前接续状态

连接恢复；12416179实际COMPLETED 0:0/2972s，完整27单元本地复算一致。
MW delta=0、MP delta=0.0009710309111506943，两臂均gained=lost=0。
按下述第4条自动关闭gene-utility扩张，不启动A、不重跑C。
[结果及完整性核验](GAP-BRIDGE-C-UTILITY-20260906-R1-RESULT.md)已归档。
下文9月6日网络阻塞记录仅为历史。跨物种总目标仍active；下一步为已有
L1/upstream证据的有限新机制提案，不把本局部负结果泛化为全部TE masking无效。

## 最新用户授权

2026-09-06用户明确要求自动推进后续实验，以构建足供学术界使用的跨物种TE掩码
基础模型为结束标准。不是以提交作业、生成协议或完成Human gap局部分支为结束。
起点固定为已提交的`12416179`，不重复提交、不覆盖readiness或冻结结果。

允许监控、取回、核对、固定验证、结果/证据/状态写入、现有协议范围的工程修复及
必要确定性检查；普通已授权步骤不重复询问。阶段性路线不清时，可基于Git提交使用
内置浏览器审阅提供建议。建议不替代新科学证据、冻结规则或明确的发布/解封人闸。

## C结果的自动处理规则（不修改evaluator）

沿用`GAP-BRIDGE-C-UTILITY-20260906-R1.md`，固定9DEV core×M0/MW/MP、27单元、
原模型/参考/mask/坐标/native-CDS evaluator。每个干预臂独立要求：
`micro-F1 delta > 0`、`gained >= 1`、`lost = 0`。

1. 先用实际Slurm终态和退出码对账，再取完整result.json、27单元输出与日志；
   核对原分母243distinct链/330来源行、TP/FP/FN微汇总、身份gain/loss及报告完整性。
2. MW通过：自动生成A下一阶段协议；覆盖、资源及代码门全部满足才允许执行。
   现有A覆盖审计显示89个crop非完整，因此不能直接启动原全候选训练。
3. 仅MP通过：自动关闭whole-gap A扩张，保留局部material路线；不把MP解释成MW通过。
4. 两臂均不通过：自动关闭此gene-utility扩张路线，不通过改门或重算口径追过线。
5. 工程失败或27单元不完整：记录工程失败并停止该实验，不作科学判断、不自动重提。
   这项具体停止规则优先于一般性的工程修复许可。
6. 连接超时不是Slurm终态，也不是科学/工程失败；保留同一job ID，恢复后继续取证。

## 总目标不能由C替代

现有L1材料路线、upstream与INIT记录说明：六物种共享模型有真实material能力，
但最弱物种的规定性能/稳定性尚未闭合。既有D为冻结baseline，INIT、B0条件路线及
旧G/R/H的停止结论保留。C只回答chr13 DEV的固定mask下游效用，不证明跨物种泛化。

总目标完成仍需以下对应范围的证据，而不是只通过局部测试：

- 明确冻结可交付模型、物种范围、原始FASTA推理/校准/阈值及适用的Label-A评价对象。
- 内部训练物种满足现行L1性能、P/R、稳定性及topology guardrails；不能仅以macro盖过最弱物种。
- 在获准开放的外部/复制/全assembly面板按对应协议验证；不得提前读取当前封存资产。
  具体原门见`CROSS-SPECIES-L1-MATERIAL-ROUTE-20260903.md`的E1/E2/E3，不随结果下调。
- 真实连续FASTA输入上的坐标、尾部、接缝、原始connected-run行为与资源可用性经验证；
  当前9个合成测试及8206bp人工序列smoke不代表真实全基因组验证。
- 研究代码、可复现实验/模型来源、依赖、使用示例、限制与许可范围闭合；
  family/同源/历史暴露未核清者如实限制主张，不写“从未见过”。
- 对外发布对象、权重/素材分发及可见性、最终封存面板开放仍按现行明确批准要求处理。
  “可供学术界使用”是目标，不等于已经授权任意对外发布或解封。

若C路线关闭，后续L1提案必须针对已经识别的完全漏检/终端缺失等主要问题提出
可区分的机制，不重开已否的初始化、阈值或同类局部规则扫描。先使用已有结果形成
有依据的有限协议；资源、数据或科学批准范围实质改变时才提出具体人闸。

## 中央状态与本次接管记录

- `ACTIVE_GOAL.json`目前仍记录旧GAP_BRIDGE_PHASE0_R1，不能将其旧指标用于C或
  本总目标的成功判定。本文件保存用户新授权的实际内容，不擅自改变该JSON状态。
- 已读取最新pursue/tri-review说明，并运行research_flow_guard：入口无blocker，
  但提示旧screen_anchor缺失。该提示不是本次固定M0对照的失败，也不是外部SOTA可比性证明。
- 2026-09-06 21:20–21:23 UTC接管：默认SSH与禁用连接复用的15秒限时SSH均连接
  `login1.baobab.hpc.unige.ch:22`超时。没有获得`12416179`的当前Slurm状态；
  本机只存在code_review_gate.json，没有科学result.json副本。
- 最后一次实际确认是本日较早时作业内8tests/full-input预检通过并开始core0 M0。
  这是历史启动证据，不是当前仍RUNNING或已经COMPLETED的证据。连接恢复后首个动作
  仍为对账`12416179`，不从旧STATUS或墙钟时间推断其结果。
