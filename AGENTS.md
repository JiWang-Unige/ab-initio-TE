# 用户驱动的科研协作

本项目已于 2026-09-08 按用户要求退出 Auto Research / lwcr 自动研究框架。用简体中文沟通。

## 目标与推进

- 目标、范围和完成标准以用户在当前对话中的明确约定为准。goal 是研究意图，不要求设置平台 goal、集群 goal 或 `ACTIVE_GOAL.json`，也不以其状态判定授权或完成。
- 已授权范围内自主完成实施、必要验证、结果处理和交付；不因进入常规下一步重复询问。提交作业不等于完成用户要求的结果分析。
- 仅在缺失信息实质改变目标、科学有效性、资源预算或授权范围时询问。既有批准在范围和条件不变时继续有效。
- 不启动已退役的自动研究流程或第三方 CLI 评阅（tri-review、council、review-board 等），除非用户明确要求；不以评阅人数或第三方登录状态阻塞研究。历史确定性验证器（如 `validate_goal.py`）可作为已授权核验／复现的实现手段，在确认无越界副作用后按需使用；其输出不作为当前授权或自动决策依据，不恢复活动 goal，也不授权新训练或阈值变更。
- Agent 仍应依据代码和结果做与任务风险相称的审阅，报告真实问题；不为了凑审查流程反复检查，不把自查冒充独立评阅。

## 科研与执行边界

- 保留实际实验协议中的数据划分、同源隔离、sealed/held-out 数据限制、评价口径、冻结阈值、预算和停止条件。取消框架流程不等于取消科学约束，也不自动授权首训、新实验、放行封存数据或对外发布。
- 根据当前任务读取相关协议、配置及结果，不要求每次通读全部历史台账。若授权或科学选择确有冲突，明确指出冲突并询问，不凭旧模板自行决定。
- 工程完成、指标可信、研究成功、泛化成立是不同判断；探索性结果不得包装为确认性结论。失败和未执行必须分开。
- 在 Baobab 上，训练、评估和重数据处理经 Slurm `sbatch`/`srun` 执行；登录节点只做轻量文件、文本、传输和调度操作。使用项目对应环境，不往 base 安装依赖；环境以实际配置和作业脚本核实。
- 保留数据、checkpoint、冻结结果和他人的未提交改动；只修改任务涉及的文件。不默认提交 Git、取消作业或创建后台监控。
- 检查必须针对具体失败，并影响下一步决策；完成相称验证后交付，不额外叠加守卫、哈希、迁移框架或审计循环。

## 历史资料与记录

- `docs/`、`reports/`、实验配置和日志继续作为科研证据。历史文本中的 Auto Research 阶段闸、goal 状态、Skill 要求和评阅阻塞记录不再是当前流程指令；其中真实数据限制和结果仍须尊重。
- 重要结果和决策在相关项目文档中简明记录即可，不要求重复写入多份固定台账。
- 旧 Auto Research 配置、Skill、hook、goal 及安装/生成入口已移到项目外备份；不要自动恢复、同步或重新安装。通用本机 Skill 按任务需要使用。
- 历史脚本及验证器保留用于复现，不是当前研究门槛。部分旧脚本仍引用已归档的 `ACTIVE_GOAL.json`；需要重跑时按具体任务迁移输入或显式使用历史合同，不重新建立活动 goal，也不把旧验证器判定当作当前授权。
- `AGENTS.md` 是项目协作规则；`CLAUDE.md` 仅指向它，不再从旧框架生成。
